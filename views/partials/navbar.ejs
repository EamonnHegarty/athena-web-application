<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Athena</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button> 
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup"> 
        <div class="navbar-nav">
          <% if(!currentUser) { %>
          <a class="nav-link" href="/">Home</a>
          <% } else if(currentUser) { %>
            <% if (currentUser.role === "employee"){ %> 
              <a class="nav-link" href="/expenses">My Expenses</a>
              <a class="nav-link" href="/expenses/new">Submit Expense</a>
            <% } else if (currentUser.role === "manager"){ %> 
              <a class="nav-link" href="/expenses">Manage Expenses</a>
            <% } else if(currentUser.role === "admin"){ %> 
              <a class="nav-link" href="/register">Add Employees</a>
              <a class="nav-link" href="/expenses/new">Submit Expense</a>
              <a class="nav-link" href="/expenses">My Expenses</a>
            <% } %> 
          <% } %> 
          </div>
        <div class="navbar-nav ms-auto">
          <% if(!currentUser) { %>
            <a class="nav-link" href="/login">Login</a>
          <% } else if(currentUser) { %> 
            <div class="nav-link"><%= currentUser.username%> (<%= currentRole %>)</div>
            <a class="nav-link" href="/logout">Logout</a>
          <% } %> 
        </div>
      </div>
    </div>
</nav>